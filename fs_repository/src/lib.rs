use entities::Todo;

pub fn save(todo: Todo) {
    println!("great success");
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
